# Android SDK Consumer Compile Smoke

This tiny source tree verifies that code outside `com.arcedge.wfblink.smoke`
can compile against the local SDK AAR. It is intentionally not a full app.

Run:

```bash
scripts/build-android-sdk-consumer-smoke.sh
```
