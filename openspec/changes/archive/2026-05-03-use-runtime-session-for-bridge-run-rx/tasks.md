## 1. Implementation

- [x] 1.1 Preserve `NeedMoreData` RX parser outcomes in runtime session reads.
- [x] 1.2 Add a diagnostic helper to process runtime-parsed RX packets.
- [x] 1.3 Route `bridge-run` RX reads through `RuntimeRadioSession::read_rx_packets`.

## 2. Validation

- [x] 2.1 Run runtime RX tests, bridge-run tests, and workspace tests.
- [x] 2.2 Validate the OpenSpec change strictly.
