## Approach

Keep the update factual and narrow:

- Do not claim the diagnostic CLI has been replaced by a production binary.
- Do claim runtime ownership of transport/session open policy, selected init helpers, MACID programming, TX submission, raw packet replay, RX bulk parsing, endpoint selection, and counters.
- Keep orchestration, CLI parsing, report formatting, RF-quality automation, and legacy smoke probes diagnostic-owned.
