# Bench Plan

This project assumes the Mac owns the AWUS036ACH as a USB peripheral. The adapter does not need to appear as a macOS Wi-Fi interface, and the first proof path is a native userspace process using `rusb`. On macOS 26, IOUSBHost direct-control diagnostics are the current fallback when IOKit sees the device but libusb cannot enumerate it.

## Hardware Assumptions

- Host: Apple Silicon Mac running macOS 15 or macOS 26.
- Primary radio: ALFA AWUS036ACH or another RTL8812AU adapter with a known VID/PID.
- Supported IDs currently include Realtek `0x0bda:0x8812`, `0x0bda:0x881a`, `0x0bda:0x881b`, `0x0bda:0x881c`, and several common RTL8812AU rebrands.
- Peer: a known-good Linux WFB-ng system with a working RTL8812AU/RTL88xxAU radio path.
- Monitor: an independent receiver or Linux monitor-mode adapter for confirming that TX frames actually radiate.

## Mac And Linux Setup

Use the Mac for the experimental userspace backend and the Linux peer as the reference WFB endpoint.

1. On the Mac, run `wfb-radio-diag usb-probe` first and record the VID/PID, bus, address, speed, interfaces, and endpoint layout.
2. If `usb-probe` cannot see the adapter on macOS, run `wfb-radio-diag macos-usb-state --vid <vid> --pid <pid>` and record the IOKit registration, matching, configuration, and interface-child state.
3. Run `wfb-radio-diag reg-smoke` to confirm userspace register reads work before any init writes are attempted. On macOS 26, use `macos-reg-smoke` if the libusb claim path is unavailable.
4. Run `wfb-radio-diag efuse-dump --i-understand-this-writes-control-registers` to capture physical EFUSE bytes, decode the logical map, and archive RFE/TX-power source bytes before tuning power behavior. On macOS 26, use `macos-efuse-dump` if the libusb claim path is unavailable.
5. Run `wfb-radio-diag led-smoke --pin led0 --mode normal --action blink --i-understand-this-writes-registers` while watching the adapter, then sweep other pin/mode pairs if the visible LED does not move.
6. Run `wfb-radio-diag power-on-smoke --i-understand-this-writes-registers` to verify the first guarded power-on/RF-reset write sequence. On macOS 26, use `macos-power-on-smoke` if the libusb claim path is unavailable.
7. Run `wfb-radio-diag llt-smoke --i-understand-this-writes-registers` to verify LLT page-chain programming without bulk traffic. On macOS 26, use `macos-llt-smoke` after `macos-power-on-smoke` if the libusb claim path is unavailable.
8. Run `wfb-radio-diag firmware-smoke --firmware <rtl8812aefw.bin> --i-understand-this-writes-registers` to verify firmware download, checksum, and readiness without bulk traffic. On macOS 26, use `macos-firmware-smoke` after `macos-power-on-smoke` if the libusb claim path is unavailable.
9. Run `wfb-radio-diag queue-dma-smoke --i-understand-this-writes-registers` to verify queue reserved pages and DMA boundaries without bulk traffic. On macOS 26, use `macos-queue-dma-smoke --bulk-out-endpoint-count <n>` after firmware and LLT if the libusb claim path is unavailable.
10. Run `wfb-radio-diag mac-smoke --i-understand-this-writes-registers` to verify MAC/WMAC setup without BB/RF setup or bulk traffic. On macOS 26, use `macos-mac-smoke` after queue/DMA if the libusb claim path is unavailable.
11. Run `wfb-radio-diag bb-smoke --i-understand-this-writes-registers` to verify BB PHY/AGC table programming without RF radio table setup, channel tuning, or bulk traffic.
12. Run `wfb-radio-diag rf-smoke --i-understand-this-writes-registers` to verify RF radioA/radioB table programming without channel tuning or bulk traffic.
13. Run `wfb-radio-diag init --channel 36 --bandwidth 20 --firmware <rtl8812aefw.bin> --i-understand-this-writes-registers` to verify the integrated power, firmware, LLT, queue/DMA, MAC, BB, RF, and 20 MHz channel path over one USB claim.
14. On Linux, confirm the same adapter model works with the chosen WFB-ng driver and channel.
15. Run `wfb-radio-diag rx-scan --channel 36 --duration-ms 1000 --pcap <path> --frame-jsonl <path>` after `init` to prove bounded bulk-IN reads, descriptor parsing, PCAP output, and raw-frame metadata emission on the selected channel.
16. Run `wfb-radio-diag tx-once --channel 36 --frame-hex <hex> --tx-status --i-understand-this-transmits` after `init` to prove one guarded descriptor-prefixed bulk-OUT submission and capture read-only TX status register deltas.
17. Run `wfb-radio-diag tx-repeat --channel 36 --count <n> --interval-ms <ms> --frame-hex <hex> --tx-status --i-understand-this-transmits` only after `tx-once` passes, starting with small counts.
18. Keep the first link one-way and low-rate: Linux transmits test WFB payloads, Mac receives and forwards to an aggregator.
19. After RX forwarding is stable, reverse the path: stock WFB-ng distributor traffic feeds the Mac bridge, and Linux verifies received payloads.
20. Only attempt sustained video after low-rate bidirectional payload counters line up.

## Linux USB Baselines

Capture Linux USB behavior before porting more initialization code. The useful traces are init, channel switch, bounded RX, and single-frame TX.

Recommended capture flow:

1. Boot the Linux reference system with the known-good driver and adapter.
2. Start a USB capture for the adapter bus with Wireshark usbmon, `tcpdump` on usbmon, or a hardware analyzer if available.
3. Record one trace for each stage: driver attach/init, set channel 36 at 20 MHz, receive a short burst, transmit one known frame.
4. Save captures with metadata: adapter VID/PID, driver fork and commit, kernel version, channel, bandwidth, rate/MCS, and WFB-ng command line.
5. Compare macOS control transfer ordering, register values, firmware download chunks, TX descriptor bytes, and bulk endpoint behavior against those traces.

## Safe Defaults

Default operation should stay conservative until captures prove otherwise.

- Channel: start on non-DFS 5 GHz channel 36 or a locally authorized 2.4 GHz channel.
- Bandwidth: 20 MHz.
- TX rate: OFDM 6 Mbps by default. `tx-once` and `tx-repeat` can use `--tx-rate` for explicit descriptor diagnostics with legacy, HT MCS, or VHT NSS/MCS rates; WFB bridge TX still follows radiotap metadata with conservative fallbacks.
- TX power: no override until the captured EFUSE power-table bytes are mapped to the Linux driver's final per-rate RF power indexes.
- Repeated TX: require explicit count, interval, channel, and authorization flag.
- SGI, LDPC, and STBC: available only behind visible `--short-gi`, `--ldpc`, and `--stbc` TX flags and echoed in JSON counters.
- LED control: the attached unit's visible blue LED is normal `led0` on `REG_LEDCFG0`; `tx-once` and `tx-repeat` can use `--tx-led` to show software TX submissions, but this is not RF proof.
- TX status sampling: `tx-once` and `tx-repeat` can use `--tx-status` to read selected RTL8812AU status registers before and after bulk-OUT submissions; this is chip-side telemetry, not RF proof.
- Wider bandwidth: keep behind explicit bandwidth selection and do not treat as verified until captures prove it.

## Known Limitations

- RTL8812AU live init now completes power-on, firmware download, LLT, queue/DMA, MAC, BB, RF, and 20/40/80 MHz channel setup. `rx-scan` can issue bounded bulk-IN reads after init, and `tx-once` can submit one descriptor-prefixed frame over bulk OUT after init.
- The first live `rx-scan` on channel 36 completed without USB errors but captured no frames: 14 read timeouts over 1.5 seconds, 0 bulk bytes, a header-only PCAP, and an empty frame JSONL. A Linux peer or known-active channel is still needed to prove actual frame reception.
- The first live `tx-once` on channel 36 completed one USB bulk-OUT write without USB errors: 24-byte frame, 64-byte descriptor-prefixed packet, endpoint `0x02`, one submitted TX frame. An independent monitor receiver is still needed to prove over-the-air radiation.
- The first live `tx-repeat` on channel 36 completed three paced USB bulk-OUT writes without USB errors: 3 submitted frames, 192 bytes written, no failed or short writes. A Linux peer or independent monitor receiver is still needed before treating this as RF throughput.
- The first live `tx-once --tx-led` and `tx-repeat --tx-led` runs toggled the visible `REG_LEDCFG0` LED around software TX submissions while preserving successful bulk-OUT counters. Reports: `/tmp/wfb-live-tx-once-led.json`, `/tmp/wfb-live-tx-repeat-led.json`.
- The first live `tx-once --tx-status` run submitted one packet and reported one read-only status delta, `REG_TXPKT_EMPTY` from `0x0fff` to `0x0ffe`. The first live `tx-repeat --tx-status` run submitted three packets and reported no changed status registers in its post-burst window. Reports: `/tmp/wfb-live-tx-once-status.json`, `/tmp/wfb-live-tx-repeat-status.json`.
- A 20 MHz USB-side burst submitted 50 of 50 frames at 1 ms requested spacing, wrote 3,200 descriptor-prefixed USB bytes in 65 ms, reported about 769 submitted frames/s, and used about 2.48 ms process CPU time. This is not packet-loss or RF throughput without a receiver.
- A flagged live `tx-once` run with `--short-gi --ldpc --stbc` completed one bulk-OUT write and reported all three descriptor flags in JSON. Peer decode of those options is not yet verified.
- LEDCFG software-control writes pass across normal LED0/1/2 plus antdiv/minicard alternatives on the attached `0x0bda:0x8812` adapter. Operator observation confirmed the visible blue enclosure LED is normal LED0 on `REG_LEDCFG0`.
- 40 MHz channel setup now has receiver-backed channel-context verification. After the Linux peer was pinned to monitor channel 36 and verified stable, Mac `bridge-tx-listen --bandwidth 40` with stock `wfb_tx -d -B 40` input injected 300/300 datagrams and Linux `wfb_rx` recovered 99/100 `STK40STA` payloads while both radios were tuned `36 HT40+`. A bounded `bridge-run --bandwidth 40` pass then forwarded 98 Linux-to-Mac WFB frames and injected 300/300 Mac-to-Linux datagrams in one retained session; Linux recovered 98/100 `MAC2L40A` payloads. A post-default run without manual TX-shape flags injected 120/120 and recovered 30 decrypted `DEF40A` source payloads with the expected Linux-shaped descriptor. Caveat: Linux monitor metadata and the Mac-side RTL8812AU RX descriptor both report the WFB MCS1 data frames as `20 MHz`, so this verifies HT40 channel operation and WFB flow, not proven wide-PPDU airtime. Reports/captures: `/tmp/wfb-agent-stock40stablea-listen.json`, `/tmp/wfb-agent-run40stablea-bridge-run.json`, `/tmp/wfb-agent-default40a-listen.json`, `/tmp/wfb-agent-rxmeta40a.json`, `/tmp/wfb-agent-rxmeta40a.jsonl`, `/tmp/mac-stock40stablea-rf.pcap`, `/tmp/mac-run40stablea-rf.pcap`.
- 80 MHz channel setup now passes live USB-level init, RX-scan, single-frame TX, and repeated-TX checks on channel 36. Reports: `/tmp/wfb-live-init-channel80.json`, `/tmp/wfb-live-rx-scan-80mhz.json`, `/tmp/wfb-live-tx-once-80mhz.json`, `/tmp/wfb-live-tx-repeat-80mhz.json`.
- Explicit VHT diagnostic rate selection now passes USB-level live TX checks: `tx-once --bandwidth 80 --tx-rate vht2ss-mcs9 --short-gi --ldpc --stbc` and matching `tx-repeat` submitted descriptor-prefixed packets and echoed the VHT rate in JSON. Reports: `/tmp/wfb-live-tx-once-vht-rate.json`, `/tmp/wfb-live-tx-repeat-vht-rate.json`.
- EFUSE dump now passes on the attached `0x0bda:0x8812` adapter and captured a decoded logical map with EFUSE USB ID `0x0bda:0x8812`, MAC `00:c0:ca:ba:bd:9f`, RFE option `0x03`, and 66 non-`0xff` bytes in the 84-byte TX-power region. Report: `/tmp/wfb-live-efuse-dump.json`.
- On macOS 26, the remote hardware Mac saw the RTL8812AU in IOKit as `!registered, !matched` with no interface children, so libusb `usb-probe` could not enumerate or claim it. The IOUSBHost fallback passed descriptor/interface/pipe smokes, register reads, EFUSE, guarded power-on, firmware, LLT, queue/DMA, MAC, BB, RF, integrated `init --macos-usbhost`, bounded `rx-scan --macos-usbhost`, single/repeated descriptor-prefixed TX through `tx-once --macos-usbhost` and `tx-repeat --macos-usbhost`, one parsed WFB distributor-style datagram through `bridge-tx-once --macos-usbhost`, and bounded UDP-received WFB distributor-style datagrams through `bridge-tx-listen --macos-usbhost`. A three-datagram listener run submitted all three packets with no drops, failures, or short writes. Reports: `/tmp/wfb-remote-macos-usb-state.json`, `/tmp/wfb-remote-macos-init-usbhost.json`, `/tmp/wfb-remote-macos-rx-scan-usbhost.json`, `/tmp/wfb-remote-macos-tx-once-usbhost.json`, `/tmp/wfb-remote-macos-tx-repeat-usbhost.json`, `/tmp/wfb-remote-macos-bridge-tx-once-usbhost.json`, `/tmp/wfb-remote-macos-bridge-tx-listen-usbhost.json`, `/tmp/wfb-remote-macos-bridge-tx-listen-3-usbhost.json`.
- Linux USB-capture comparison found that the bridge same-session init must follow Linux's LLT-before-firmware order. With that order enabled in the bridge init helpers, a UDP-fed `bridge-tx-listen --macos-usbhost --init-before-tx` run submitted 40/40 WFB-shaped data frames and the Linux monitor captured 39 `LISTENORD` payload markers. Reports/captures: `/tmp/wfb-agent-listen-linuxorder.json`, `/tmp/mac-listen-linuxorder-rf.pcap`.
- Bridge TX now defaults HT/VHT WFB distributor datagrams to the Linux monitor-injection descriptor shape: management queue, MACID 1, HT rate-ID 7 for HT MCS, fallback enabled, fallback limit 0, and no aggregate break. This removes the need for the earlier manual `--tx-queue mgnt --mac-id 1 --tx-rate-id 7 --enable-rate-fallback --no-agg-break` flags on normal bridge runs. Use `--tx-profile radiotap-direct` when an experiment needs the unmodified radiotap-derived queue/rate/fallback shape. A post-profile live smoke injected 90/90 datagrams and reported `tx_profile=linux_monitor`; Linux `wfb_rx` recovered 29/30 `PROF20F` source payloads. The working network path for that run used the hardware Mac LAN address `10.42.0.162` and a temporary UDP relay to bridge-localhost because direct UDP to the hardware Mac Tailscale address timed out in this bench state. Report: `/tmp/wfb-agent-profile20f-listen.json`.
- Controlled stock WFB-ng TX now passes in the Mac-to-Linux direction. With the Linux peer's channel manager stopped temporarily and `wfb0` pinned to channel 36/HT20, stock `wfb_tx -d` sent distributor datagrams to Mac `bridge-tx-listen`; the Mac injected 300/300, the Linux monitor captured 298 WFB MAC frames, and dedicated Linux `wfb_rx` forwarded 99 decrypted `STOCKCTRL` payloads to localhost. Reports/captures: `/tmp/wfb-agent-stock-controlled-listen.json`, `/tmp/mac-stock-controlled-rf.pcap`, `/tmp/mac-stock-controlled-rx-lo.pcap`.
- The first receiver-backed 20 MHz baseline uses stock `wfb_tx -d -k 8 -n 12` with 512-ish source payloads. Mac `bridge-tx-listen` received and injected 1,200/1,200 distributor datagrams, wrote 723,735 descriptor-prefixed USB bytes in 14.896 s, reported 80.56 submitted datagrams/s, 48.58 KB/s USB write rate, and 1.43% of one CPU core. The Linux monitor captured 1,169 WFB MAC frames, and dedicated Linux `wfb_rx` forwarded 796 decrypted `SIZE512` payload markers. Reports/captures: `/tmp/wfb-agent-size512-listen.json`, `/tmp/mac-size512-rf.pcap`, `/tmp/mac-size512-rx-lo.pcap`.
- Payload-size bracketing showed that the configured `-l 1000` range is healthy. Runs with 768, 900, and exactly 1,000 byte source payloads each injected 600/600 distributor datagrams and recovered 400/400 decrypted receiver payloads. A longer exactly-1,000-byte run injected 3,000/3,000 datagrams, captured 2,906 WFB MAC frames, and recovered 1,999/2,000 decrypted payloads. The earlier 1024-ish source-payload failure was above the `wfb_tx/wfb_rx -l 1000` setting, not a Mac radio TX ceiling inside the configured MTU. Reports/captures: `/tmp/wfb-agent-size768c-listen.json`, `/tmp/wfb-agent-size900-listen.json`, `/tmp/wfb-agent-size1000-listen.json`, `/tmp/wfb-agent-sust1000-listen.json`, `/tmp/mac-sust1000-rf.pcap`, `/tmp/mac-sust1000-rx-lo.pcap`.
- The longer 1,000-byte run is sustained packet-flow evidence, not yet a real video/telemetry workload: 92.24 submitted distributor datagrams/s, 100.58 KB/s USB write rate, and 1.32% of one CPU core for the Mac bridge.
- The first full-duplex `bridge-run` loop now works over one retained radio session. The command preserves station/MSR state for TX, opens RX filter maps, drains UDP TX input with socket receiver thread(s), interleaves queued TX with bulk-IN RX reads, and reports combined RX/TX counters. It defaults to bounded diagnostics, but `--duration-ms 0` removes the time bound and `--max-datagrams 0` removes the TX datagram cap for longer bridge runs; SIGINT/SIGTERM exits through the normal report path with `stop_reason="signal"`. A live SIGTERM smoke at `/tmp/wfb-agent-bridge-run-signal2.json` exited with `result=pass`, `stop_reason=signal`, no error, and preserved RX counters. A bidirectional run forwarded 44 Linux-to-Mac WFB frames to a Mac aggregator socket while injecting 121/121 Mac-to-Linux distributor datagrams; Linux `wfb_rx` recovered 80/80 `MAC2LIN` payloads and the Linux monitor captured 120 WFB MAC frames. Reports/captures: `/tmp/wfb-agent-bridge-run-duplex3.json`, `/tmp/wfb-agent-bridge-run-signal2.json`, `/tmp/wfb-agent-bridge-run-duplex3-agg.json`, `/tmp/mac-bridgerun-duplex3-rf.pcap`, `/tmp/mac-bridgerun-duplex3-rx-lo.pcap`.
- The production `radio-run` adapter now has a local full-duplex smoke with the adapter on this Mac: `/tmp/wfb-radio-run-duplex-local-a3/radio-run.json` passed, submitted `149/149` Mac-to-Linux frames with zero drops, forwarded `242` Linux-to-Mac WFB frames, and both peer counters recovered `80/80` marked payloads. The reusable runner `scripts/run-radio-run-duplex-smoke.sh` reproduced this at `/tmp/wfb-radio-run-duplex-script-a1`, with both counters `80/80`, `256` forwarded RX frames, and `149/149` submitted TX frames. The run also confirmed the Linux WFB-ng CLI must receive decimal link IDs (`1`), while reports can continue to show `0x000001`.
- After promoting detailed RX forwarding snapshots into `radio-run` JSON, local validation at `/tmp/wfb-radio-run-duplex-forward-detail-20260504-135348` passed the production loop and emitted `rx.rx_forwards[0].counters.forwarded=131` with no send failures. The bench placement had moved from about 1 ft to about 6 ft and was less clean than the earlier baseline: both peer counters recovered `76/80`, and the Linux peer logged 4 Mac-to-Linux packet losses. Treat this artifact as production-report-shape validation, not RF-quality baseline evidence.
- The duplex smoke runner now emits `smoke_result` and exits nonzero when either peer counter misses its configured minimum, when TX drops/fails, or when `radio-run` does not expose RX forwarding snapshots. Strict validation at `/tmp/wfb-radio-run-duplex-strict-20260504-140119` correctly failed the current 6 ft bench placement (`74/80` Mac-to-Linux, `77/80` Linux-to-Mac) while still showing a clean production loop (`radio_result=pass`, TX `149/149`, no drops, RX forward snapshots present).
- The same strict runner can now exercise write-authorized calibration profiles. On this bench, `TX_CALIBRATION_PROFILE=rtl8812a-lck` stayed in the same near-pass/fail band (`78/80` Mac-to-Linux, `76/80` Linux-to-Mac at `/tmp/wfb-radio-run-duplex-lck-strict-20260504-140606`). `TX_CALIBRATION_PROFILE=rtl8812a-runtime-iqk` is a regression for TX quality: Linux-to-Mac recovered `80/80`, but Mac-to-Linux recovered `0/80` at `/tmp/wfb-radio-run-duplex-iqk-strict-20260504-140426`, with Linux `wfb_rx` reporting unable-decrypt packets despite `radio-run` submitting `149/149` frames.
- Runtime IQK now skips all final IQC fill writes unless every TX/RX path completed in the selected sweep. Retest `/tmp/wfb-radio-run-duplex-iqk-allornothing-20260504-141407` recovered `80/80` Mac-to-Linux with `runtime_iqk.status=completed`, proving the fallback-fill regression is fixed. The strict duplex gate still failed because Linux-to-Mac recovered only `69/80` in the current 6 ft bench, so this is a safety correction rather than an RF baseline pass.
- Stronger FEC helps only the Mac-to-Linux side in the current placement. With current-default calibration and `FEC_K=8 FEC_N=16`, `/tmp/wfb-radio-run-duplex-fec16-strict-20260504-141743` recovered `80/80` Mac-to-Linux and submitted `197/197` frames, but still failed Linux-to-Mac at `70/80`. That keeps the open issue on Mac-side RX quality/bench geometry rather than TX queueing or distributor ingress.
- Runtime IQK reports now include pre-sweep BB/RF/TXAGC state plus raw per-attempt IQC register readbacks. Evidence run `/tmp/wfb-radio-run-duplex-iqk-evidence-20260504-143429` completed IQK in sweep 2 with cleanup restored and no fallback stages in the selected sweep, but receiver-backed counters still failed (`72/80` Mac-to-Linux, `69/80` Linux-to-Mac). A peer driver-reload usbmon baseline at `/tmp/wfb-linux-iqk-driver-reload-20260504-143841` captured 7,607 RTL8812AU register writes and shows the Linux driver writes static IQK result registers (`0x0c58/0x0e58 = 0x30000c1c`, `0x0c5c/0x0e5c = 0x00000058`) plus RFE pinmux flips, but did not show the same dynamic page-C1 TX/RX IQC fill sequence. Runtime IQK remains experimental until the dynamic fill behavior is reconciled with Linux driver behavior.
- Peer hardening now verifies Linux `wfb0` is monitor/radiotap immediately before traffic, forces tcpdump to `IEEE802_11_RADIO`, collects partial peer artifacts on startup failure, and counts Linux `wfb_rx` decrypt failures in the strict summary. The first hardened `TX_POWER_MODE=efuse-derived` run at `/tmp/wfb-radio-run-duplex-default-hardened-20260504-145458` showed a state-sensitive TX corruption signature (`149/149` Mac TX frames submitted, `0/80` recovered, 125 decrypt failures), but a repeat at `/tmp/wfb-radio-run-duplex-efuse-full-20260504-150316` recovered `80/80` Mac-to-Linux with zero decrypt failures while Linux-to-Mac was `77/80`. `TX_POWER_MODE=current-default` at `/tmp/wfb-radio-run-duplex-no-txpower-20260504-145725` recovered `80/80` Mac-to-Linux and `79/80` Linux-to-Mac. The reusable smoke runner now defaults to current-default TX power; EFUSE-derived TX power remains explicit and receiver-gated until sustained repeats prove it is stable.
- Runtime IQK cleanup now backs up and restores every RF offset and page-C1/normal BB register family touched by the TX/RX one-shot sequence. Before this fix, `rtl8812a-runtime-iqk` with current-default TX power fell back and corrupted Mac-to-Linux (`0/80`, 123 decrypt failures at `/tmp/wfb-radio-run-duplex-iqk-currenttx-20260504-150531`). After the expanded restore set, `/tmp/wfb-radio-run-duplex-iqk-restorefull-20260504-151048` completed IQK in sweep 2, restored 14 MAC/BB, 12 AFE, 8 RF-per-path, and 20 page-C1 registers, logged zero decrypt failures, and recovered `75/80` Mac-to-Linux plus `77/80` Linux-to-Mac.
- Runtime IQK selected IQC fill now runs after cleanup instead of before cleanup, and `radio-run` refuses live TX if runtime IQK fallback remains, cleanup fails, or selected IQC fill was not applied. The negative fallback run `/tmp/wfb-radio-run-duplex-iqk-fill-after-restore-20260504-152042` still submitted `149/149` frames and produced `0/80` Mac-to-Linux with 135 decrypt failures, proving fallback must be a pre-TX failure state. The completed-fill run `/tmp/wfb-radio-run-duplex-iqk-fill-passcheck-20260504-152638` completed IQK in sweep 1, applied 20 post-cleanup IQC fill writes, logged zero decrypt failures, and recovered `77/80` Mac-to-Linux plus `80/80` Linux-to-Mac. Runtime IQK remains receiver-gated and not a distance default until repeated close-range and range A/B show margin improvement over current-default.
- Runtime IQK now has a larger production `radio-run` close-range gate: `/tmp/wfb-rfq-runtime-iqk-fill-2000-20260504-153130/rf-quality-report.json` completed IQK in sweep 2, applied 20 post-cleanup fill writes, recovered `1999/2000` marked payloads with zero decrypt failures, and matched the Linux baseline recovery (`loss_delta_percent_points=0.0`, `within_margin`). This is the current best runtime-IQK correctness artifact, but still not a long-distance acceptance because the receiver SNR field is zero-only and the geometry is the local 6 ft bench.
- The paired current-default comparator at `/tmp/wfb-rfq-current-default-2000-20260504-153545/rf-quality-report.json` recovered `2000/2000` marked payloads, logged zero decrypt failures, and beat the Linux baseline by one payload (`loss_delta_percent_points=-0.05`, `within_margin`) on the same local 6 ft production flow. Runtime IQK is now corrected and safe enough for A/B, but it has not shown close-range margin improvement over captured/default calibration.
- The duplex smoke runner now writes `summary.json` even when `radio-run` exits before the ready marker because calibration failed closed. Startup summaries carry the radio exit status, log tail, runtime IQK status, cleanup state, selected-IQC-fill applied flag, and fill register count, so a calibration gate failure is distinguishable from a generic readiness timeout.
- Live validation of the hardened summary path passed at `/tmp/wfb-radio-run-duplex-iqk-summary-gate-20260504-154808`: runtime IQK completed in sweep 2, cleanup restored, selected IQC fill applied with 20 writes, `radio-run` submitted `149/149` TX frames, Linux logged zero decrypt failures, Mac-to-Linux recovered `80/80`, and Linux-to-Mac recovered `76/80` under the current lenient 6 ft smoke gate.
- Production RX reports now include `rx.signal` with adapter-side RSSI, SNR, and noise sample counts plus min/max/rounded-average dB values. Live validation at `/tmp/wfb-radio-run-duplex-signal-summary-20260504-155712` populated the summary over `6991` samples (`rssi_dbm.avg=-64`, `snr_db.avg=10`, `noise_dbm.avg=-74`) while both peer counters recovered `20/20` with zero decrypt failures. This is report plumbing only; long-distance decisions still require receiver-backed Linux RX_ANT telemetry and controlled geometry.
- `bridge-run` now supports multiple TX bind sockets and multiple RX forwarding targets in one radio session. A four-stream video/telemetry-shaped run used Linux-to-Mac WFB ports 0 and 1 plus Mac-to-Linux ports 2 and 4. With `--tx-burst-limit 8`, the Mac bridge injected 423/423 distributor datagrams with no drops, Linux `wfb_rx` recovered 120/120 `M2LVID` markers and 79/80 `M2LTEL` markers, and the Mac forwarded 120 port-0 frames plus 69 port-1 frames to separate aggregator sockets. Reports/captures: `/tmp/wfb-agent-bridge-run-multibridge3.json`, `/tmp/wfb-agent-bridge-run-multibridge3-agg.json`, `/tmp/mac-multibridge3-rf.pcap`, `/tmp/mac-multibridge3-rx-lo.pcap`.
- `bridge-run` now drains startup bursts sent during retained-session radio init. Live follow-up `/tmp/wfb-agent-bridgerun-drain2.json` used a hardware-Mac LAN relay while Linux `wfb_tx -d` sent distributor traffic before the main bridge loop was active; the Mac bridge drained and submitted 90/90 datagrams, and Linux `wfb_rx` recovered 30/30 `DRAIN2` source payloads. One relay datagram arrived beyond the command's `--max-datagrams 90` bound and was intentionally left unsubmitted.
- RX forwarding has also been verified against stock WFB-ng aggregator network mode. Linux `wfb_tx` sent 120 source payloads on radio port 0, Mac `bridge-run` forwarded 138 matching WFB frames to Linux `wfb_rx -a 5800` over UDP, and that real aggregator recovered 120/120 decoded `L2MRXAG` payloads on localhost. Report/capture: `/tmp/wfb-agent-bridge-run-rxagg1.json`, `/tmp/mac-rxagg1-agg-lo.pcap`.
- Real encoded H.264/RTP traffic now crosses the Linux-to-Mac WFB RX path. Local `ffmpeg` generated a 10 s, 320x180@15 fps H.264 RTP stream at about 271 kb/s, Linux `wfb_tx` transmitted it on radio port 0, Mac `bridge-run` matched and forwarded 446 WFB frames to Linux `wfb_rx -a`, and Linux emitted 318 RTP packets toward the hardware Mac. A UDP counter on the hardware Mac received 289 RTP packets / 184,585 bytes with payload type 96. Direct Linux-to-local-Mac UDP did not arrive at `100.112.15.116` during the bench, so decoded-output tests should target the hardware Mac Tailscale address `100.104.12.123` or add an explicit relay. Reports/captures: `/tmp/wfb-agent-bridge-run-video4.json`, `/tmp/wfb-video4-udp-recv.json`, `/tmp/mac-video4-agg-to-hwmac.pcap`, `/tmp/wfb-video4-ffmpeg-send.log`.
- BE-queue full-width HT40 data remains non-working. A BE-queue descriptor with `DATA_BW=1` and `DATA_SC=0` either wedged around the queue-fill boundary or, after clearing `REG_DATA_SC_8812` to `0x00`, submitted 120/120 without USB failures but produced zero decrypted receiver payloads. The management-queue path with the same global `REG_DATA_SC_8812=0` still decoded, but still showed MCS1 20 MHz on the Linux monitor. Reports/captures: `/tmp/wfb-agent-be40widea-listen.json`, `/tmp/wfb-agent-be40sc0a-listen.json`, `/tmp/wfb-agent-mgnt40sc0a-listen.json`, `/tmp/be40widea-rf.pcap`, `/tmp/be40sc0a-rf.pcap`, `/tmp/mgnt40sc0a-rf.pcap`.
- EFUSE-derived final per-rate RF power indexes are implemented but remain receiver-gated; use decrypt-failure and sustained-payload evidence before enabling them for distance work.
- The duplex smoke runner now supports per-direction enablement, FEC, and Linux transmitter MCS knobs, and records those fields in `summary.json`. Indoor 100 ft exploration through several doors found the default `8/12` profile unusable, symmetric `4/12` at 20 ms workable only as a short smoke, and the best short profile as asymmetric M2L `4/12` MCS1 plus L2M `3/12` MCS2 at 20 ms. Two 80-payload runs recovered `80/80` plus `76/80` and `80/80` plus `78/80` with zero decrypt failures, but the 200-payload follow-up recovered only `181/200` plus `164/200` and logged 66 L2M decrypt failures. Treat `/tmp/wfb-radio-run-distance-100ft-asym-m2l4x12-l2m3x12-mcs2-200p-interval20ms-20260504-164549` as evidence that the profile is promising but not accepted for production range.
- `scripts/run-radio-run-profile-matrix.sh` now wraps the production duplex smoke into repeatable profile matrices. It supports local or remote hardware Mac execution, optional rsync deploy, built-in `minimal`/`short`/`range` profile sets, custom `PROFILE_FILE` matrices, per-run artifact collection, and ranked JSON/Markdown summaries. The underlying smoke uses batch-mode SSH timeouts, detaches remote SSH stdin so one remote run cannot consume the remaining profile list, and writes structured startup summaries for Linux peer preparation or traffic failures. In the current remote-Mac topology, `LINUX_LAN_IP=10.42.0.1` is required because that is the Linux peer address visible from `rownds-macbook-pro`; using `192.168.122.77` caused Linux-to-Mac forwarding to report `0/80` even while Mac-to-Linux RF worked.
- Short-range remote matrix validation now has an accepted sustained profile. With the adapter back on `rownds-macbook-pro`, asymmetric M2L `4/12` MCS1 plus L2M `3/12` MCS2 at a 20 ms source interval passed a single 200-payload run at `/tmp/wfb-radio-profile-matrix-remote-asym-200-20260504-184452`. A two-repeat run with only 20 unmeasured warmup exposed a startup acquisition failure in one repeat (`172/200` L2M, missing measured sequences `0..27`, 121 L2M decrypt failures) at `/tmp/wfb-radio-profile-matrix-remote-asym-200-repeat2-20260504-184700`. Re-running the same profile with `SOURCE_WARMUP_PAYLOADS=100` passed two of two sustained repeats at `/tmp/wfb-radio-profile-matrix-remote-asym-200-warm100-repeat2-20260504-185114`: both directions recovered `200/200`, decrypt failures were zero, and TX drops/failures were zero. Treat this as the current short-range production smoke default, not a long-distance acceptance.
- Direct `rusb` ownership may be blocked if macOS attaches another driver or changes USB ownership behavior.
- The first bridge path is not a fake macOS network interface; it speaks WFB-ng distributor and aggregator wire formats directly.
- PCAP output is raw IEEE 802.11 linktype, not radiotap, until RX metadata is rich enough to justify radiotap capture output.
- IOUSBHost can reach default-control transfers and retained bulk pipes on macOS 26 even when libusb cannot see the adapter. USBDriverKit becomes necessary if IOUSBHost retained sessions become unreliable for sustained bridge traffic, deployment requires a signed system extension, or macOS 26 changes make userspace interface ownership unstable.
